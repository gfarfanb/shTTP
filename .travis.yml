sudo: required

language: bash

branches:
  only:
    - master

install:
  - curl --version
  - jq --version
  - shellcheck --version
  - sudo apt-get install nginx

script:
  - ./lint
  - sudo ./runtests
