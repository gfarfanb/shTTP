#  shTTP mock services configuration
#  -author Giovanni Farfán B.

server {
    listen 5555 default_server;
    listen [::]:5555 default_server;

    root /var/www/shttp_mock;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}
